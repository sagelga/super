import React from 'react';

export const runtime = 'edge';

const BlogPostPage = () => {
    return (
        <div>
            <h1>Blog Post Page</h1>
        </div>
    );
};

export default BlogPostPage;